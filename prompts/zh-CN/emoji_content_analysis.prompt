输入类型：{image_type}
这是一个表情包图片。输入类型为 GIF 时，图片由动态图中抽取的不同帧横向拼接而成；输入类型为 STATIC 时，图片为单张静态图。
请分析这个表情包，输出 3-5 个标签，用逗号分隔。

要求：
1. 前 2-3 个标签必须是情绪、态度或语气相关标签，例如：委屈、无语、崩溃、自嘲、得意。
2. 最后补充 1-2 个角色/IP/主体标签，用于概括画面内容，例如：奶龙、猫猫虫咖波、熊猫头、小猫等人物或角色的名字；实在不明确时再概括外表特征。
3. 只允许输出简短的标签词，不要输出完整句子、场景描述、解释或梗句。
4. 必须且只能输出这些标签词和逗号，不要添加任何前缀（如“[情绪]”）或其他内容。
